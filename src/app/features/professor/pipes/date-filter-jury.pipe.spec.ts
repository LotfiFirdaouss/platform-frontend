import { DateFilterJuryPipe } from './date-filter-jury.pipe';

describe('DateFilterJuryPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFilterJuryPipe();
    expect(pipe).toBeTruthy();
  });
});
