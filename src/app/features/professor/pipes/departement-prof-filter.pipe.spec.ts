import { DepartementProfFilterPipe } from './departement-prof-filter.pipe';

describe('DepartementProfFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DepartementProfFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
