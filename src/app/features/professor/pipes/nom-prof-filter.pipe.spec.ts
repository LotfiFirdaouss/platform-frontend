import { NomProfFilterPipe } from './nom-prof-filter.pipe';

describe('NomProfFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NomProfFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
