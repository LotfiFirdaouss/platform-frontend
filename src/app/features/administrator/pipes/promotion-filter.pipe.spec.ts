import { PromotionFilterDashboardPipe } from './promotion-filter.pipe';

describe('PromotionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PromotionFilterDashboardPipe();
    expect(pipe).toBeTruthy();
  });
});
