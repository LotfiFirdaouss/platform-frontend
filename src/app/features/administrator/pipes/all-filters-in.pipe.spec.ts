import { AllFiltersInPipe } from './all-filters-in.pipe';

describe('AllFiltersInPipe', () => {
  it('create an instance', () => {
    const pipe = new AllFiltersInPipe();
    expect(pipe).toBeTruthy();
  });
});
