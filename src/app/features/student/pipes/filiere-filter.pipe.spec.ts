import { FiliereStudentFilterPipe } from './filiere-filter.pipe';

describe('FiliereStudentFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FiliereStudentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
