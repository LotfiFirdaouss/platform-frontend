import { PromotionStudentFilterPipe } from './promotion-filter.pipe';

describe('PromotionStudentFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PromotionStudentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
