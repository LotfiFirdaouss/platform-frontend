import { NomStudentFilterPipe } from './nom-filter.pipe';

describe('NomStudentFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NomStudentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
