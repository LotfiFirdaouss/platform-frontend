import { ConfidentielFilterPipe } from './confidentiel-filter.pipe';

describe('ConfidentielFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ConfidentielFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
