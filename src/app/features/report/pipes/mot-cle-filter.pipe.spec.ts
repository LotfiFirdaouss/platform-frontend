import { MotCleFilterPipe } from './mot-cle-filter.pipe';

describe('MotCleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MotCleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
