import { ReportStatsNamePipe } from './report-stats-name.pipe';

describe('ReportStatsNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ReportStatsNamePipe();
    expect(pipe).toBeTruthy();
  });
});
