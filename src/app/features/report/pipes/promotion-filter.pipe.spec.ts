import { PromotionFilterPipe } from './promotion-filter.pipe';

describe('PromotionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PromotionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
