import { VillePaysFilterPipe } from './ville-pays-filter.pipe';

describe('VillePaysFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VillePaysFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
