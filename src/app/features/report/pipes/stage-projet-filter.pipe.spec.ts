import { StageProjetFilterPipe } from './stage-projet-filter.pipe';

describe('StageProjetFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StageProjetFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
