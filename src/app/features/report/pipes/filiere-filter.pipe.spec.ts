import { FiliereFilterPipe } from './filiere-filter.pipe';

describe('FiliereFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FiliereFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
