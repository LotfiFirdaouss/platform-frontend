import { SocieteFilterPipe } from './societe-filter.pipe';

describe('SocieteFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SocieteFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
