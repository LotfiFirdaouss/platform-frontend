import { SecteurSocieteFilterPipe } from './secteur-societe-filter.pipe';

describe('SecteurSocieteFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SecteurSocieteFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
