import { SocieteInsertionFilterPipe } from './societe-insertion-filter.pipe';

describe('SocieteInsertionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SocieteInsertionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
