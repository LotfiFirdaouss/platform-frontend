import { UnivInsertionFilterPipe } from './univ-insertion-filter.pipe';

describe('UnivInsertionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UnivInsertionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
