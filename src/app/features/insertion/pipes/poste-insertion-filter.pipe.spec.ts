import { PosteInsertionFilterPipe } from './poste-insertion-filter.pipe';

describe('PosteInsertionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PosteInsertionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
