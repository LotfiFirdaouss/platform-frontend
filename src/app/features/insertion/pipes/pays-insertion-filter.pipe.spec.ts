import { PaysInsertionFilterPipe } from './pays-insertion-filter.pipe';

describe('PaysInsertionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PaysInsertionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
