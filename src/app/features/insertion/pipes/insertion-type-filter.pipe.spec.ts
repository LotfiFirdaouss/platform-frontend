import { InsertionTypeFilterPipe } from './insertion-type-filter.pipe';

describe('InsertionTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new InsertionTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
