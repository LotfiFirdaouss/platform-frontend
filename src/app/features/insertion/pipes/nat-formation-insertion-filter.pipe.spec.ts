import { NatFormationInsertionFilterPipe } from './nat-formation-insertion-filter.pipe';

describe('NatFormationInsertionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NatFormationInsertionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
