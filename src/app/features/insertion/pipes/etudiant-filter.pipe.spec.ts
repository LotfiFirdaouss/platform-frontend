import { EtudiantFilterPipe } from './etudiant-filter.pipe';

describe('EtudiantFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EtudiantFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
