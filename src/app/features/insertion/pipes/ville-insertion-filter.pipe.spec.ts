import { VilleInsertionFilterPipe } from './ville-insertion-filter.pipe';

describe('VilleInsertionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VilleInsertionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
