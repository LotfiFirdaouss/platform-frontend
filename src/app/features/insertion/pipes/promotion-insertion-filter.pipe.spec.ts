import { PromotionInsertionFilterPipe } from './promotion-insertion-filter.pipe';

describe('PromotionInsertionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PromotionInsertionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
